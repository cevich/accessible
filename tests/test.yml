---

- hosts: all
  gather_facts: False
  strategy: linear
  vars_files:
    - '{{ playbook_dir }}/test_vault.yml'
  roles:
    - role: cevich.accessible
      vars:
        rhsm: '{{ _vault_rhsm if ansible_host == "subtest" else {} }}'
